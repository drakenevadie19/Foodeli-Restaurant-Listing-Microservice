eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  datasource:
    #db name: restaurantdb
    # url: jdbc:mysql://localhost:3306/restaurantdb
    # username: root
    # password: "@Thanhnguyen14#"
    # driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:postgresql://db.sqselqywbqxngsisvoll.supabase.co:5432/postgres?user=postgres&password=@Thanhnguyen14#
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      # if there is update on data, it will update existing tables, not to create a new one
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    #    Convert based language to my SQL based query
    # database-platform: org.hibernate.dialect.MySQL5InnoDBDialect